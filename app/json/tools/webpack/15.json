{"sta":1,"data":{"subTitle":"splitChunks","content":"<p>从 webpack v4 开始，移除了&nbsp;<code>CommonsChunkPlugin</code>，取而代之的是&nbsp;<code>optimization.splitChunks</code>。<br></p><p>配置</p><pre><code>optimization: {\n        // 开启最小化,默认是true\n        minimize: true,\n        minimizer: [new TerserPlugin({\n            exclude:/nodu_modules/\n        })],\n        //\n        splitChunks:{\n            chunks: \"all\", //表明将选择哪些 chunk 进行优化,默认作用于异步chunk，值为all/initial/async\n            minSize: 0, //默认值是30kb,代码块的最小尺寸\n            minChunks: 1, //被多少模块共享,在分割之前模块的被引用次数\n            maxAsyncRequests: 3, //限制异步模块内部的并行最大请求数的，说白了你可以理解为是每个import()它里面的最大并行请求数量\n            maxInitialRequests: 5, //限制入口的拆分数量\n            name: true, //打包后的名称，默认是chunk的名字通过分隔符（默认是～）分隔开，如vendor~\n        }\n    },</code></pre><p><br></p>"}}