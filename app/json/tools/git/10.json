{"sta":1,"data":{"subTitle":"撤销","content":"<p></p><pre><code><span spellcheck=\"true\"># 恢复暂存区的指定文件到工作区\n</span>$ git checkout <span>[</span>file<span>]</span>\n\n<span spellcheck=\"true\"># 恢复某个commit的指定文件到暂存区和工作区\n</span>$ git checkout <span>[</span>commit<span>]</span> <span>[</span>file<span>]</span>\n\n<span spellcheck=\"true\"># 恢复暂存区的所有文件到工作区\n</span>$ git checkout <span>.</span>\n\n<span spellcheck=\"true\"># 重置暂存区的指定文件，与上一次commit保持一致，但工作区不变\n</span>$ git reset <span>[</span>file<span>]</span>\n\n<span spellcheck=\"true\"># 重置暂存区与工作区，与上一次commit保持一致\n</span>$ git reset <span>--</span>hard\n\n<span spellcheck=\"true\"># 重置当前分支的指针为指定commit，同时重置暂存区，但工作区不变\n</span>$ git reset <span>[</span>commit<span>]</span>\n\n<span spellcheck=\"true\"># 重置当前分支的HEAD为指定commit，同时重置暂存区和工作区，与指定commit一致\n</span>$ git reset <span>--</span>hard <span>[</span>commit<span>]</span>\n\n<span spellcheck=\"true\"># 重置当前HEAD为指定commit，但保持暂存区和工作区不变\n</span>$ git reset <span>--</span>keep <span>[</span>commit<span>]</span>\n\n<span spellcheck=\"true\"># 新建一个commit，用来撤销指定commit\n</span><span spellcheck=\"true\"># 后者的所有变化都将被前者抵消，并且应用到当前分支\n</span>$ git revert <span>[</span>commit<span>]</span>\n\n<span spellcheck=\"true\"># 暂时将未提交的变化移除，稍后再移入\n</span>$ git stash\n$ git stash pop</code></pre>"}}